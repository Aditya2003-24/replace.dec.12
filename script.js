let mydata = `google is a great place to work.
 google offers servies and google gives many amenities to the employees`;


//i is for case-sensitive search g is for global searching
let newData = mydata.replace(/google/ig,"amazon");
console.log(newData);
//BOM,DOM,VARIABLE,FUCTIONS ,ES6 FEATURES,OBJECTS,ARRAY,MATH METHOD,STRING METHOD
//BOM=BROWSER OBJECT MODEL (WINDOW OBJECT).